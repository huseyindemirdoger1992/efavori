@using data
@using data._Product
@using System.Globalization
@using System.Security.Claims
@using Microsoft.AspNetCore.Http
@using Microsoft.EntityFrameworkCore
@using System.Linq

@{
    Layout = "~/Areas/_Layout.cshtml";

    // --- 1. Dil belirleme ---
    string currentLang = "en";
    var supportedCultures = new[] { "tr", "en", "az", "de", "es", "fr", "hi", "pt", "ru", "zh" };
    bool isLangSetByUser = false; // Dilin veritabanından gelip gelmediğini kontrol etmek için

    // --- 2. Controller'dan Gelen Ürünü Doğrudan Alıyoruz (EKSTRA DB SORGUSUNA GEREK YOK) ---
    Products? ProductProfile = ViewBag.ProductProfile as Products;

    // --- 3. Kullanıcı Oturum ve Dil Kontrolü ---
    var userEmail = User.FindFirstValue(ClaimTypes.Email);
    Users? use = null;

    // Sadece kullanıcı oturum açmışsa veritabanına kullanıcı/dil için bağlanıyoruz
    if (!string.IsNullOrEmpty(userEmail))
    {
        using (var db = new _ApplicationConnectionDb())
        {
            use = await db.Users
                .AsNoTracking()
                .FirstOrDefaultAsync(u => u.ContactInformation.Email == userEmail);

            // Kullanıcının kayıtlı bir dili varsa onu kullan
            if (use != null && !string.IsNullOrEmpty(use.Language) && supportedCultures.Contains(use.Language.ToLower()))
            {
                currentLang = use.Language.ToLower();
                isLangSetByUser = true;
            }
        }
    }

    // --- 4. Ziyaretçi Dil Kontrolü (Oturum kapalıysa veya kullanıcının dili seçili değilse) ---
    if (!isLangSetByUser)
    {
        // Önce URL'deki Route parametresine bak
        var routeLang = ViewContext.RouteData.Values["culture"]?.ToString()?.ToLower();
        if (!string.IsNullOrEmpty(routeLang) && supportedCultures.Contains(routeLang))
        {
            currentLang = routeLang;
        }
        else
        {
            // Route boşsa Cookie'den oku
            var httpContext = Context;
            if (httpContext != null && httpContext.Request.Cookies.TryGetValue(".Efavori.Culture", out var cookieValue) && !string.IsNullOrEmpty(cookieValue))
            {
                // ASP.NET Core formatı: c=tr|uic=tr
                var parts = cookieValue.Split('|');
                var cPart = parts.FirstOrDefault(p => p.StartsWith("c="));
                if (cPart != null)
                {
                    var langCode = cPart.Substring(2).ToLower();
                    if (supportedCultures.Contains(langCode))
                    {
                        currentLang = langCode;
                    }
                }
            }
        }
    }
}

@if (!User.Identity.IsAuthenticated)
{
    @* Oturum Açmamış*@
    {
        switch (currentLang)
        {
            case "tr":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                    RenderMode.Server,
                    new
                    {
                        use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId
                    }))
                ;
                break;
            case "az":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "de":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "es":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "fr":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "hi":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "pt":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "ru":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "zh":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            default:
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
        }
    }
}
else
{
    @* Oturum Açmış*@
    {
        switch (currentLang)
        {
            case "tr":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "az":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "de":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "es":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "fr":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "hi":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "pt":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "ru":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            case "zh":
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
            default:
                @(await Html.RenderComponentAsync<razor._Shared.tr.Pages.Public._Viewer.ArticleDetail>(
                                                RenderMode.Server,
                                                new { use = (use != null ? use : null), ArticleId = (Guid)ViewBag.ArticleId }))
                ;
                break;
        }
    }
}